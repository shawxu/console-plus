{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome - file",
            "request": "launch",
            "type": "chrome",
        //    "file": "${workspaceFolder}/${relativeFile}",
            "file": "${file}",
            "runtimeArgs": [
                "--allow-file-access-from-files"
            ]
        }
    ]
}